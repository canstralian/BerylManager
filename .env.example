# Database Configuration
# PostgreSQL connection string for production (use Neon or local PostgreSQL)
DATABASE_URL=postgresql://username:password@localhost:5432/beryl_manager

# Session Configuration
# Secret key for session encryption (use a strong random string in production)
SESSION_SECRET=your-secret-key-change-in-production

# Server Configuration
# Port for the application server (default: 5000)
PORT=5000

# Node Environment
# Options: development, production, test
NODE_ENV=development
